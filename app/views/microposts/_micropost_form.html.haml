= form_for(@micropost, html: { multipart: true, class: 'form' }) do |f|
  = render 'shared/error_messages', object: f.object
  .form-group
    = f.text_area :content, placeholder: 'Compose new micropost...', class: 'form-control'
    %span.picture
      = f.file_field :picture, accept: 'image/jpeg,image/gif,image/png'
  .form-group
    = f.button '<span class="glyphicon glyphicon-pencil"> Post</span>'.html_safe, class: 'btn btn-add-post'
  = javascript_include_tag 'microposts'
